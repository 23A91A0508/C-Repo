#include <stdio.h>

int main() {
    int X, Y, Z;

    // Read the input values
    scanf("%d %d", &X, &Y);

    // Calculate the second number
    Z = 2 * X - Y;

    // Output the result
    printf("%d\n", Z);

    return 0;
}